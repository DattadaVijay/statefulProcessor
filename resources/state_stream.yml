resorces:
  pipelines:
    stateful_processor_test:
      name: ${bundle.name}_stateful_stream_pipeline_${bundle.target}
      catalog: assetbundles
      serverless: true
      schema: default

      libraries:
        - file:
            path: ../src/data_gen.py
      
      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
